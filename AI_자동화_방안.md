# 한국외대 맛집 지도 - AI Agent 기반 자동화 방안

## 📋 현재 상황 분석

### 현재 데이터 수집 방식
- **서울캠**: 선배의 카카오지도 저장 맛집 기반 크롤링
- **글로벌캠**: 네이버지도/에타 맛집게시판 크롤링 또는 수동 입력
- **문제점**: 
  - 선배 졸업 시 데이터 소스 단절
  - 수동 업데이트의 시간 소모
  - 졸업 후 지속적 운영의 어려움

### 기술 스택
- 프론트엔드: 정적 HTML + Tableau 대시보드
- 데이터 저장: Tableau (시각화), JavaScript 배열 (랜덤메뉴)

---

## 🎯 AI Agent 자동화 솔루션

### 1. 전체 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│              AI Agent 자동화 시스템                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐      ┌──────────────┐                │
│  │  웹 크롤링   │      │  데이터 추출 │                │
│  │   Agent      │ ───▶ │   Agent      │                │
│  └──────────────┘      └──────────────┘                │
│         │                      │                        │
│         │                      ▼                        │
│         │              ┌──────────────┐                │
│         │              │  데이터 검증 │                │
│         │              │   Agent      │                │
│         │              └──────────────┘                │
│         │                      │                        │
│         └──────────────────────┼──────────────────────┘
│                                ▼                        │
│                        ┌──────────────┐                │
│                        │  데이터베이스 │                │
│                        │   (JSON/DB)   │                │
│                        └──────────────┘                │
│                                │                        │
│                                ▼                        │
│                        ┌──────────────┐                │
│                        │  Tableau/     │                │
│                        │  웹사이트     │                │
│                        │   업데이트    │                │
│                        └──────────────┘                │
└─────────────────────────────────────────────────────────┘
```

---

## 🛠️ 구체적 구현 방안

### 방안 1: Playwright + AI Agent (추천)

#### 기술 스택
- **Playwright**: 브라우저 자동화
- **OpenAI GPT-4 / Claude**: 텍스트 추출 및 구조화
- **LangChain / AutoGPT**: Agent 프레임워크
- **Python**: 백엔드 스크립트

#### 구현 단계

**1단계: 웹 크롤링 Agent**
```python
# 예시 구조
class RestaurantCrawlerAgent:
    def __init__(self):
        self.browser = playwright.chromium.launch()
        self.page = self.browser.new_page()
    
    def crawl_naver_map(self, location="이문동"):
        # 네이버 지도 접속
        # 검색어 입력: "이문동 맛집"
        # 맛집 리스트 클릭
        # 각 맛집 상세 페이지 방문
        # 텍스트 데이터 추출
        pass
    
    def crawl_kakao_map(self, location="이문동"):
        # 카카오맵 접속
        # 유사한 프로세스
        pass
```

**2단계: 데이터 추출 Agent**
```python
class DataExtractionAgent:
    def __init__(self):
        self.llm = OpenAI()  # 또는 Claude
    
    def extract_restaurant_info(self, raw_text):
        prompt = f"""
        다음 텍스트에서 맛집 정보를 추출해주세요:
        {raw_text}
        
        다음 형식으로 JSON으로 반환:
        {{
            "name": "음식점 이름",
            "address": "주소",
            "phone": "전화번호",
            "rating": "평점",
            "reviews": ["한줄평1", "한줄평2", ...],
            "category": "음식 카테고리",
            "price_range": "가격대"
        }}
        """
        return self.llm.extract(prompt)
```

**3단계: 스케줄링 및 자동화**
```python
# GitHub Actions 또는 cron job으로 주기적 실행
# 매주 일요일 새벽 2시에 자동 실행
schedule.every().sunday.at("02:00").do(run_crawling_agent)
```

---

### 방안 2: MCP (Model Context Protocol) 활용

#### MCP 서버 구성
```python
# mcp_restaurant_server.py
from mcp import Server

class RestaurantMCPServer:
    def __init__(self):
        self.server = Server("restaurant-crawler")
        self.setup_tools()
    
    def setup_tools(self):
        @self.server.tool()
        async def search_restaurants(location: str, category: str = None):
            """특정 지역의 맛집 검색"""
            # 네이버/카카오맵 API 호출 또는 크롤링
            pass
        
        @self.server.tool()
        async def extract_reviews(restaurant_url: str):
            """음식점의 리뷰 추출"""
            pass
        
        @self.server.tool()
        async def validate_data(restaurant_data: dict):
            """추출된 데이터 검증"""
            pass
```

#### MCP 클라이언트 (AI Agent)
```python
# AI Agent가 MCP 서버를 통해 도구 사용
agent = AIAgent(mcp_server_url="http://localhost:8000")
result = await agent.use_tool("search_restaurants", location="이문동")
```

---

### 방안 3: Playwright + Browser Automation + LLM

#### 통합 솔루션
```python
from playwright.async_api import async_playwright
from openai import OpenAI
import json

class AutomatedRestaurantAgent:
    def __init__(self):
        self.llm = OpenAI()
        self.restaurants = []
    
    async def run(self):
        async with async_playwright() as p:
            browser = await p.chromium.launch(headless=False)
            page = await browser.new_page()
            
            # 네이버 지도 접속
            await page.goto("https://map.naver.com")
            
            # AI가 페이지를 분석하고 다음 액션 결정
            page_content = await page.content()
            action = self.llm.decide_action(page_content)
            
            # 액션 실행 (검색, 클릭 등)
            await self.execute_action(page, action)
            
            # 맛집 리스트 추출
            restaurants = await self.extract_restaurants(page)
            
            # 각 맛집 상세 정보 수집
            for restaurant in restaurants:
                detail = await self.get_restaurant_details(page, restaurant)
                self.restaurants.append(detail)
            
            await browser.close()
        
        # 데이터 저장
        self.save_data()
    
    def save_data(self):
        # JSON 파일로 저장 또는 데이터베이스에 저장
        with open("restaurants.json", "w", encoding="utf-8") as f:
            json.dump(self.restaurants, f, ensure_ascii=False, indent=2)
```

---

## 📦 데이터 저장 및 관리

### 데이터 구조
```json
{
  "campus": "seoul" | "global",
  "restaurants": [
    {
      "id": "unique_id",
      "name": "음식점 이름",
      "address": "주소",
      "coordinates": {
        "lat": 37.123,
        "lng": 127.456
      },
      "phone": "02-1234-5678",
      "category": "한식",
      "rating": 4.5,
      "review_count": 150,
      "reviews": [
        {
          "text": "맛있어요!",
          "rating": 5,
          "date": "2024-01-15"
        }
      ],
      "price_range": "10000-20000",
      "opening_hours": "11:00-22:00",
      "source": "naver_map",
      "last_updated": "2024-01-15T10:30:00Z"
    }
  ]
}
```

### 데이터베이스 옵션
1. **JSON 파일**: 간단하지만 확장성 낮음
2. **SQLite**: 경량 DB, 파일 기반
3. **PostgreSQL**: 프로덕션 환경에 적합
4. **Firebase/Supabase**: 서버리스 옵션

---

## 🔄 자동화 워크플로우

### 주간 업데이트 프로세스

```
1. 트리거 (매주 일요일 새벽 2시)
   ↓
2. AI Agent 시작
   ↓
3. 네이버지도 크롤링
   ├─ 이문동 지역 검색
   ├─ 맛집 리스트 수집
   └─ 각 맛집 상세 페이지 방문
   ↓
4. 카카오맵 크롤링 (동일 프로세스)
   ↓
5. 데이터 추출 및 구조화
   ├─ LLM을 통한 정보 추출
   ├─ 중복 제거
   └─ 데이터 검증
   ↓
6. 기존 데이터와 비교
   ├─ 신규 맛집 추가
   ├─ 폐업 맛집 제거
   └─ 정보 업데이트
   ↓
7. 데이터 저장
   ↓
8. Tableau 대시보드 업데이트
   ↓
9. 웹사이트 자동 배포 (GitHub Pages)
   ↓
10. 완료 알림 (이메일/슬랙)
```

---

## 🚀 구현 우선순위

### Phase 1: MVP (최소 기능 제품)
- [ ] Playwright 기반 네이버지도 크롤링
- [ ] 기본 데이터 추출 (이름, 주소, 평점)
- [ ] JSON 파일로 데이터 저장
- [ ] 수동 실행 스크립트

**예상 시간**: 1-2주

### Phase 2: AI 통합
- [ ] LLM을 통한 리뷰 추출 및 요약
- [ ] 데이터 검증 로직
- [ ] 중복 제거 알고리즘

**예상 시간**: 1주

### Phase 3: 자동화
- [ ] GitHub Actions 스케줄링
- [ ] 자동 데이터 업데이트
- [ ] Tableau 자동 업데이트 연동

**예상 시간**: 1주

### Phase 4: 고도화
- [ ] MCP 서버 구축
- [ ] 다중 소스 크롤링 (에타, 인스타그램 등)
- [ ] 실시간 모니터링 대시보드

**예상 시간**: 2-3주

---

## 💡 실용적 팁

### 1. 크롤링 시 주의사항
- **Rate Limiting**: 너무 빠른 요청은 IP 차단 위험
- **User-Agent 설정**: 봇으로 인식되지 않도록
- **에러 핸들링**: 네트워크 오류, 페이지 변경 대응

### 2. 비용 관리
- **LLM API 비용**: GPT-4는 비쌈, GPT-3.5-turbo 또는 Claude Haiku 고려
- **서버 비용**: GitHub Actions는 무료 (공개 레포), VPS 필요 시 저렴한 옵션 선택

### 3. 법적 고려사항
- **로봇 배제 표준 (robots.txt)**: 준수 여부 확인
- **이용약관**: 각 플랫폼의 크롤링 정책 확인
- **개인정보**: 리뷰 작성자 정보는 제외

---

## 📚 추천 도구 및 라이브러리

### 크롤링
- **Playwright**: 최신 브라우저 자동화
- **Selenium**: 대안 (하지만 Playwright 추천)
- **BeautifulSoup**: HTML 파싱

### AI/LLM
- **OpenAI API**: GPT-4, GPT-3.5-turbo
- **Anthropic Claude**: Claude 3 Haiku (비용 효율적)
- **LangChain**: Agent 프레임워크
- **LlamaIndex**: 데이터 추출에 특화

### 자동화
- **GitHub Actions**: CI/CD 및 스케줄링
- **Python Schedule**: 로컬 스케줄링
- **Airflow**: 복잡한 워크플로우 관리

### 데이터 관리
- **Pandas**: 데이터 처리
- **SQLite/PostgreSQL**: 데이터베이스
- **Tableau API**: 대시보드 자동 업데이트

---

## 🎓 학습 리소스

1. **Playwright 공식 문서**: https://playwright.dev/python/
2. **LangChain 튜토리얼**: https://python.langchain.com/
3. **MCP 스펙**: https://modelcontextprotocol.io/
4. **GitHub Actions**: https://docs.github.com/en/actions

---

## 🔮 향후 확장 가능성

1. **사용자 피드백 통합**: 웹사이트에서 직접 리뷰 제출
2. **실시간 알림**: 새 맛집 추가 시 알림
3. **추천 시스템**: 사용자 취향 기반 맛집 추천
4. **모바일 앱**: React Native로 앱 개발
5. **커뮤니티 기능**: 외대생 전용 맛집 커뮤니티

---

## 📝 결론

AI Agent 기반 자동화를 통해:
- ✅ **지속 가능한 운영**: 졸업 후에도 자동 업데이트
- ✅ **시간 절약**: 수동 작업 최소화
- ✅ **데이터 품질 향상**: 일관된 데이터 수집
- ✅ **확장성**: 다른 지역/대학으로 확장 가능

**다음 단계**: Phase 1부터 시작하여 점진적으로 기능을 추가하는 것을 권장합니다.



